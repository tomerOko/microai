console.log('controllerErrorHandling.ts');
/**
 * explain about each controller function should have a try catch block and error handling
 * to convert the AppError ro ResponseError and send it in the next function instead of throwing it
 */
